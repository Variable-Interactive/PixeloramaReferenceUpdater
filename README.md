# Reference Updater

Auto updates reference images whenever the application regains focus, or when project is switched back.

This extension also provides a button in project menu as well for manually updating reference images
